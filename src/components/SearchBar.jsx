import TextField from "./UI/TextField";

function SearchBar() {
  return (
    <div>
      <h1>Search by name</h1>
      <TextField placeholder="Write the name..." />
    </div>
  );
}

export default SearchBar;
